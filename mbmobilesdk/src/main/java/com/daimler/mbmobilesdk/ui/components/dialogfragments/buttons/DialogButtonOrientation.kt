package com.daimler.mbmobilesdk.ui.components.dialogfragments.buttons

/**
 * Orientation options for buttons in a dialog fragment.
 */
internal enum class DialogButtonOrientation {
    /** Buttons are ordered horizontally. */
    HORIZONTAL,

    /** Buttons are ordered vertically. */
    VERTICAL
}